import React, { Component } from 'react';

export default class Sampling extends Component {
    render() {
        return(
            <div className="container">
                Sampling list via Carla
            </div>
        );
    }
}