import React, { Component } from 'react';

export default class Sensors extends Component {
    render() {
        return(
            <div className="container">
                Sensors listed by group (HOBO Sonde Campbell)
            </div>
        );
    }
}