import React, { Component } from 'react';

export default class MySite extends Component {
    render() {
        return(
            <div className="container">
                This will be the individual site page: Maplet, Deployment lists, HOBO list, Campbell list
            </div>
        );
    }
}